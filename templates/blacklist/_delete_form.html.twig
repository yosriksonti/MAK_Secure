<form method="post" action="{{ path('blacklist_delete', {'id': blacklist.id}) }}"onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ blacklist.id) }}">
    <button class="btn"><i class="fa-solid fa-trash"></i></button>
</form>
